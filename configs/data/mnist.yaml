# MNIST dataset configuration
_target_: modern_vit.data.mnist_datamodule.MNISTDataModule

# Dataset paths
data_dir: ${paths.data_dir}

# Data loading configuration
batch_size: 64
num_workers: 4
pin_memory: true

# Image configuration
image_size: 28
channels: 1
num_classes: 10
